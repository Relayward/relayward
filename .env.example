RELAYWARD_VERSION=0.2.2
RELAYWARD_BIND_ADDRESS=127.0.0.1
RELAYWARD_PORT=8080

# Optional: used only when the command override in compose.yaml is enabled.
# RELAYWARD_EVENT_HOT_RETENTION=24h
# RELAYWARD_EVENT_ARCHIVE_RETENTION=2160h

# Optional: used only when the cloudflared service in compose.yaml is enabled.
# CLOUDFLARE_TUNNEL_TOKEN=
